import "./FallBack.css";

export const FallBack = () => {
  return (
    <section className="fall-back">
      <p>fallback (404 not found)</p>
      {/* TO-DO: traer el error por parámetro, asi lo muestro acá */}
    </section>
  );
};
